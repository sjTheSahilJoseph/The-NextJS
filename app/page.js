// In nextjs, we have Image tag from Nextjs,
// Which will optimize the image internal images.
// And if we want to use external image, we have to specify the url in next.config.mjs file.

export default function Home() {
  return (
    <>
    Home
    </>
  );
}
